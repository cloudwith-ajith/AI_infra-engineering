Starting With the DBMS
# session 1 we can create the cmd using two method
/*  
1.  SQL create database
2.  SQL drop database
3.  SQL create table
4.  SQL insert info to table
5.  SQL Drop table
*/
show databases;
use world;

show tables;

select * from city;
select * from countrylanguage;
select * from country;

# create the database cmd -- create database db_name

create database custormer;

use custormer;

#To show thw table present in the db
   
show tables;
 
# to delect/deop the db 

drop database custormer;

create database employee;

show databases;

use employee;

create table employee_info(id integer,first_name varchar(10),last_name varchar(10));

show tables;

select * from employee_info 

-----------------------------------day2------------------------------------
show databases; 

use employee;

show tables;

select * from employee_info ;

insert into employee_info(id,first_name,last_name) values(1,"Ajith", "Kumar");
insert into employee_info(id,first_name,last_name) values(2,"Ajay", "Kumar");
insert into employee_info(id,first_name,last_name) values(3,"pavan", "Kumar");

# To drop the table -- drop table_name

drop table employee_info;

# To drop database ---- drop database db_name

drop database employee;

show databases

-------------------------------------NULL,UPDATE,DELETE,ALTER-------------------

SHOW databases;

create database student;

use student;

drop table student_info;

create table student_info(id integer auto_increment, student_name varchar(15), mark integer,primary key(id));

insert into student_info(student_name,mark)
values
("Ajith kumar", 99),
("Anil kumar",89),
("vijay",79),
("Gokul",90),
("Manish",79),
("Pavan",Null);

show tables;

select * from student_info;

# To get the null values

--------------------------------NOT NULL, UNIQUE,PRIMARY KEY,CONSTRAINT VARIABLE PRIMARY KEY(ID, PHONE NUMBER)


show databases;

use student;

show tables;
select * from student_info;

create table Instagram_login(
id integer not null,
user_name varchar(15) not null,
email varchar(20) not null,
age integer,
phone_number integer,
sex varchar(8));

select * from Instagram_login;

alter table Instagram_login
modify phone_number varchar(10);

insert into Instagram_login values 
(1,"ajith","ajith@gmail.com",null,8220419685,"male"),
(2,"vijay","vijay@gmail.com",31,null,"male");

show tables;

desc instagram_login;

#unique

alter table instagram_login
add unique(id);

alter table instagram_login
add unique(phone_number);

alter table instagram_login
add  constraint pk_key primary key(id,phone_number);
select * from student_info where mark is null
-----------------------------------------joins and stored produec-----
show databases;

use student;

show tables;

select * from instagram_login;

select * from student_info;

/*
Joins 
inner join 
left join
right join 
Full join 
natural join
cross join 
*/

# inner join 
show tables;

select * from instagram_login;

select * from student_info;

# update name using the update key word
# this is used to update the name bcz of the differ in the table 
update student_info set student_name = "vijay"
where id = 2;

------------------------left join 
# left join
select s.student_name,i.user_name,i.phone_number, s.mark 
from instagram_login i  left join student_info s on i.id = s.id; 


