Starting With the DBMS
# session 1 we can create the cmd using two method
/*  
1.  SQL create database
2.  SQL drop database
3.  SQL create table
4.  SQL insert info to table
5.  SQL Drop table
*/
show databases;
use world;

show tables;

select * from city;
select * from countrylanguage;
select * from country;

# create the database cmd -- create database db_name

create database custormer;

use custormer;

#To show thw table present in the db
   
show tables;
 
# to delect/deop the db 

drop database custormer;

create database employee;

show databases;

use employee;

create table employee_info(id integer,first_name varchar(10),last_name varchar(10));

show tables;

select * from employee_info 

-----------------------------------day2------------------------------------
show databases; 

use employee;

show tables;

select * from employee_info ;

insert into employee_info(id,first_name,last_name) values(1,"Ajith", "Kumar");
insert into employee_info(id,first_name,last_name) values(2,"Ajay", "Kumar");
insert into employee_info(id,first_name,last_name) values(3,"pavan", "Kumar");

# To drop the table -- drop table_name

drop table employee_info;

# To drop database ---- drop database db_name

drop database employee;

show databases

-------------------------------------NULL,UPDATE,DELETE,ALTER-------------------

SHOW databases;

create database student;

use student;

drop table student_info;

create table student_info(id integer auto_increment, student_name varchar(15), mark integer,primary key(id));

insert into student_info(student_name,mark)
values
("Ajith kumar", 99),
("Anil kumar",89),
("vijay",79),
("Gokul",90),
("Manish",79),
("Pavan",Null);

show tables;

select * from student_info;

# To get the null values

--------------------------------NOT NULL, UNIQUE,PRIMARY KEY,CONSTRAINT VARIABLE PRIMARY KEY(ID, PHONE NUMBER)


show databases;

use student;

show tables;
select * from student_info;

create table Instagram_login(
id integer not null,
user_name varchar(15) not null,
email varchar(20) not null,
age integer,
phone_number integer,
sex varchar(8));

select * from Instagram_login;

alter table Instagram_login
modify phone_number varchar(10);

insert into Instagram_login values 
(1,"ajith","ajith@gmail.com",null,8220419685,"male"),
(2,"vijay","vijay@gmail.com",31,null,"male");

show tables;

desc instagram_login;

#unique

alter table instagram_login
add unique(id);

alter table instagram_login
add unique(phone_number);

alter table instagram_login
add  constraint pk_key primary key(id,phone_number);
select * from student_info where mark is null
-----------------------------------------joins and stored produec-----
show databases;

use student;

show tables;

select * from instagram_login;

select * from student_info;

/*
Joins 
inner join 
left join
right join 
Full join 
natural join
cross join 
*/

# inner join 
show tables;

select * from instagram_login;

select * from student_info;

# update name using the update key word
# this is used to update the name bcz of the differ in the table 
update student_info set student_name = "vijay"
where id = 2;

------------------------left join 
# left join
select s.student_name,i.user_name,i.phone_number, s.mark 
from instagram_login i  left join student_info s on i.id = s.id; 

-------------------------------stored procedures
# stored procedures, like function in programming language 
/*
CREATE DEFINER=`root`@`localhost` PROCEDURE `get_insta`()
BEGIN
select * from instagram_login;
END

key words --> call 
parameters (IN,OUT,INOUT);
*/
call get_insta("female");
call get_insta("male");

show databases;

use student;

show tables;

drop table instagram_login;

drop table student_info;

drop database student;

create database demo;

show databases;

use demo;

CREATE TABLE Designation (
    design_id INT PRIMARY KEY,
    design_name VARCHAR(50),
    department VARCHAR(50)
);

INSERT INTO Designation VALUES 
(101, 'Manager', 'Sales'),
(102, 'Senior Engineer', 'Engineering'),
(103, 'Data Analyst', 'Analytics'),
(104, 'HR Specialist', 'Human Resources'),
(105, 'Intern', 'Engineering');


CREATE TABLE Employee (
    emp_id INT PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    salary INT,
    hire_date DATE,
    designation_id INT,
    FOREIGN KEY (designation_id) REFERENCES Designation(design_id)
);

INSERT INTO Employee VALUES 
(1, 'Amit', 'Sharma', 85000, '2021-03-15', 101),
(2, 'Priya', 'Patel', 95000, '2020-06-01', 102),
(3, 'Rahul', 'Verma', 70000, '2022-01-10', 103),
(4, 'Sneha', 'Reddy', 65000, '2021-11-20', 104),
(5, 'Vikram', 'Singh', 30000, '2023-05-01', 105),
(6, 'Rohan', 'Das', 72000, '2022-08-14', 103);

CREATE TABLE Bonus (
    bonus_id INT PRIMARY KEY,
    emp_id INT,
    bonus_amount INT,
    bonus_date DATE,
    FOREIGN KEY (emp_id) REFERENCES Employee(emp_id)
);

INSERT INTO Bonus VALUES 
(501, 1, 5000, '2023-12-25'),
(502, 2, 7000, '2023-12-25'),
(503, 3, 4000, '2023-12-25'),
(504, 1, 3500, '2024-06-15'),
(505, 4, 4500, '2023-12-25');

show tables;

select * from bonus;
desc bonus;
select * from designation;
desc designation;
select * from employee;
desc employee;

select * from employee
where first_name like '%riy%';

select concat(first_name,last_name) as employee_name,salary from employee
where salary> 50000 and salary < 80000;

select * from employee
where hire_date > "2021-01-01";

select * from employee
where year(hire_date)  = 2021;

select * from employee
where month(hire_date) = 11;
use department;
select count(*) as number_count,department from designation
group by department;

-----------------------------
select * from employee;
select * from designation;

select * from employee inner join designation
on employee.designation_id = designation.design_id
where design_name ="Intern";


create table Employee_clone as
select * from employee;

show tables;

select * from Employee_clone;
#fourth highest salary using the limit n-1,1;
select * from employee
order by salary desc limit 4 ; 
#sixth highest salary
select * from employee
order by salary desc limit 5,1;






